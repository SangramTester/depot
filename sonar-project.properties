# Organization and project keys are displayed in the right sidebar of the project homepage
sonar.organization=sangramtester
sonar.projectKey=SangramTester_depot2
sonar.host.url=https://sonarcloud.io

sonar.scm.provider=git
# sonar.pullrequest.provider=GitHub


sonar.exclusions=**/*.java,**/*.jar,./app/assets/**/*
sonar.ruby.coverage.reportPaths=coverage/.resultset.json


# sonar.pullrequest.base=master
# sonar.pullrequest.branch=master/rubocop-test
# sonar.pullrequest.key=4


sonar.exclusions=**/*.java,**/*.jar,./app/assets/**/*
# sonar.ruby.coverage.reportPaths=coverage/.resultset.json
# sonar.ruby.rubocop.reportPaths=rubocop-result.json
# --- optional properties ---

# defaults to project key
#sonar.projectName=My project
# defaults to 'not provided'
#sonar.projectVersion=1.0
 
# Path is relative to the sonar-project.properties file. Defaults to .
# sonar.sources=./app
sonar.sources=app, config, db
 
# Encoding of the source code. Default is default system encoding
#sonar.sourceEncoding=UTF-8
